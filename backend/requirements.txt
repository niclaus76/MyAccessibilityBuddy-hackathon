# Core dependencies for MyAccessibilityBuddy Backend
# Install with: pip install -r requirements.txt

# FastAPI and web server
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.9

# Web scraping and HTML parsing
beautifulsoup4>=4.14.2
requests>=2.32.5

# Image processing
Pillow>=12.0.0
piexif>=1.1.3

# SVG support (optional but recommended)
CairoSVG>=2.8.2

# LLM providers
openai>=2.8.1
anthropic>=0.40.0  # Claude (Anthropic) API client
google-genai>=0.1.0  # Google Gemini API client (new SDK)
ollama>=0.3.0  # Ollama client for local models
# ecb-llm-client>=0.8.6  # Optional: ECB internal package, install separately if needed or uncomment this row

# Environment variables
python-dotenv>=1.2.1

# Note: All sub-dependencies will be installed automatically
# This file contains only the direct dependencies required by the application
